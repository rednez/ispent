version: '3.9'

services:
  db:
    image: postgres:latest
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    environment:
      POSTGRES_PASSWORD: ${ISPENT_POSTGRES_PASS}
      POSTGRES_DB: 'ispent'

volumes:
  postgres-data:
